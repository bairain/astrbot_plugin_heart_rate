# Changelog

本文件记录 astrbot_plugin_heart_rate（心率监测）插件的版本变更。

## v1.0.0

首个正式版本。

### 新增

- AI 问候的多模型自动兜底：生成心率问候时优先使用当前选中的对话模型，失败后自动依次尝试所有已配置的对话模型，避免某个模型服务不可用（如接口返回 404）时问候生成失败。
- `CHANGELOG.md`，消除 AstrBot 加载时的 changelog 缺失警告。

### 修复

- 告警评估循环健壮性：单次评估异常（如模型接口抖动）不再导致整个告警循环退出，异常仅记录日志并在下一个评估周期继续，避免阈值提醒在插件重载前永久静默。

### 说明

- 对话模型接口名称对齐 AstrBot 4.x：使用 `Context.get_all_providers()` 获取全部已配置模型（修复 `get_providers` 属性不存在导致的告警链路报错）。

## v0.2.0

### 新增

- 🤖 AI 人设问候模式：开启 `use_llm` 后，心率越界/恢复事件会唤醒对话模型，结合当前默认人设生成符合角色口吻的主动问候（类似 AstrBot 未来任务），支持过高、过低、恢复正常三种场景的独立任务提示词。
- AI 生成失败（模型未配置、超时、返回空）时自动回退固定模板，保证提醒必达。
- 并发生成通过异步锁串行化，一次心率事件只请求一次模型并广播到所有绑定会话。

## v0.1.0

首个版本。

### 功能

- GATT 连接模式：自动扫描并连接广播标准心率服务（0x180D）的设备，订阅心率测量通知（0x2A37），支持 UINT8/UINT16 心率解析，断线自动重连。
- 广播被动接收模式：不建立连接，从 BLE 广播包 Service Data 中提取心率。
- 心率过高/过低阈值检测，越界后向所有已绑定会话主动推送消息。
- 防骚扰机制：重复提醒冷却、恢复余量判定、恢复正常通知。
- 指令：`/hr`、`/hr_scan`、`/hr_start`、`/hr_stop`、`/hr_bind`、`/hr_unbind`、`/hr_threshold`、`/hr_test`。
- 绑定会话持久化（AstrBot KV 存储，重启不丢失）。
- WebUI 可视化配置（`_conf_schema.json`）。
